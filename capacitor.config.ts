import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.gamez0.lemonadetycoon',
  appName: 'Lemonade Tycoon',
  webDir: 'dist'
};

export default config;
