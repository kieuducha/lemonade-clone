import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.kieuducha.lemonadeclone',
  appName: 'Lemonade Tycoon',
  webDir: 'dist'
};

export default config;
